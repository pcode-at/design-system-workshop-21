import {
  ArgsTable,
  Meta,
  Canvas,
  Story,
} from "@storybook/addon-docs/blocks";
import { config, withDesign } from "storybook-addon-designs";
import ProductGridItem from ".";
import Button from "../Button";
import Icon from "../Icon";
import productImage from "./productImageSample.jpg";


<Meta title="Components/ProductGridItem" parameters={{
    viewport: {
      defaultViewport: 'mobile1'
    },
    design: config({
        type: "figma",
        url:
          "https://www.figma.com/file/AWWkZAdBsBvZJHu6PnjbtP/Workshop-design-file-1?node-id=264%3A397",
      }),
  }}/>

export const ProductGridItemArgs = {
  name: "Delicious Vanilla Cupcake",
  image: productImage,
  price: "€2.00",
  description: "Sweets",
};

## ProductGridItem

ProductGridItem component displays product name, image and price.

<Canvas>
  <Story name="Small" args={ProductGridItemArgs} >
    {(args) => <ProductGridItem size="small" {...args} />}
  </Story>
  <Story name="Medium" args={ProductGridItemArgs}>
    {(args) => <div style={{maxWidth: "140px"}}>
<ProductGridItem size="medium" {...args} />
</div>
}
  </Story>
  <Story name="Big" args={ProductGridItemArgs}>
    {(args) => <div style={{maxWidth: "295px"}}><ProductGridItem size="big" {...args} /></div>}
  </Story>
</Canvas>


## Props

<ArgsTable of={ProductGridItem} />
